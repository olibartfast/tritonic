#pragma once
// Forwarding header — canonical definition is in tritonic/infra/config.hpp
#include "tritonic/infra/config.hpp"

// Backward-compatibility alias (global namespace)
using InferenceConfig = tritonic::infra::InferenceConfig;
