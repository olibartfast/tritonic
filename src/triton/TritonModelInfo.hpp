#pragma once
// Forwarding header — canonical definition is in tritonic/triton/model_info.hpp
#include "tritonic/triton/model_info.hpp"

// Backward-compatibility alias (global namespace)
using TritonModelInfo = tritonic::triton::ModelInfo;
