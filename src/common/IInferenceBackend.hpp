#pragma once
// Forwarding header — canonical definition is in tritonic/core/interfaces.hpp
#include "tritonic/core/interfaces.hpp"
#include "CommonTypes.hpp"

// Backward-compatibility alias (global namespace)
using IInferenceBackend = tritonic::core::IInferenceBackend;
